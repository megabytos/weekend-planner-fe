'use client';

import Container from '@/components/layout/container';
import PlannerContainer from '@/components/layout/planer-container';

export default function PlannerPage() {
  return (
    <Container className="py-5 md:py-8">
      <PlannerContainer />
    </Container>
  );
}
